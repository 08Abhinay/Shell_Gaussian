# Cleanup Archive 2026-06-22

These files were moved out of the active `scripts` and `configs` folders during the standardized v2 cleanup.

They are archived instead of deleted so older experiments can be recovered if needed.
